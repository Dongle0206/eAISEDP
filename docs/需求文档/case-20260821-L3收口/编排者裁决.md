# 编排者裁决记录 — case-20260821-L3收口

> 裁决人：team-orchestrator | 日期：2026-08-21 | 依据：PRD v1.0 开放问题 Q1~Q9
> 原则：对齐 V4~V6 与 L2 收口先例。

| Q | 议题 | 裁决 |
|---|------|------|
| Q1 | 表名与计算列落库 | `t_risk` / `t_compliance_check` / `t_business_case`。**计算列落库**（risk_value/risk_level/roi/payback_years/rice_score 由 Service 重算覆盖后存储——AC 防伪造 + 列表排序/聚合直接用列；对齐 t_project.progress 算好落库先例） |
| Q2 | 金额单位 | 存储单位=**元**，`DECIMAL(14,2)`（两位小数）；前端展示万/亿缩写 |
| Q3 | 状态机 | 风险：open→mitigating→closed 单向（closed 必填处置说明；不设 reopen）。案例：draft→approved→rejected→executing→done 单向；rejected/executing/done 终态；draft 可编辑，approved 后只读（仅可追加决策记录文本）；**不允许回退/reopen/批准后撤销**（审计留痕承担追溯） |
| Q4 | 关联战略存储 | 存 **id**（JSON 数组，如 `[1,5]`）——t_strategy 无 code 列、t_program.strategy_id 存 id 先例（PO 代码级证据采纳） |
| Q5 | 热力图实现 | **纯 CSS Grid**（离线渲染，技术雷达纯 CSS 先例，不引库） |
| Q6 | PM 授权 | PM：risk create/edit（一线上报）+ bizcase create/edit（起草）；**不给** compliance create/edit（防自查自登）与 bizcase:approve（防自我批准） |
| Q7 | approve 独立原子 | 采纳 `bizcase:approve`（仅 platform_admin/tenant_admin） |
| Q8 | 组合汇总口径 | 总投入/总预期收益排除 draft/rejected（含 approved/executing/done） |
| Q9 | 逾期口径与错误码 | 逾期判定=当前 **DATE**（不含时分）> 复评/复检日期，展示层高亮不落库；无新增专用错误码，非法流转/非法枚举走 400 BizException |

## 附带约束（下发 SE/DBA）
1. V7 迁移：对齐 V6 幂等规范（CREATE TABLE IF NOT EXISTS + INSERT IGNORE seed，零 ALTER 或 ALTER 必须 information_schema 动态判断）；permission 1071~1080、role_permission 2170~2202（PRD 契约）
2. 三表均为租户级业务表（不进 IGNORE_TABLES），不限层（LayerGuard 零注册）；全库 uk/索引按拦截器改写后真实 SQL 论证
3. 写操作全审计；计算列 Service 重算（客户端提交值一律覆盖）
4. 先例代码：governance 包（V6 四域）为直接模板——包结构/枚举/Controller/前端页面对齐
5. H2 schema 同步追加（schema-h2.sql）
