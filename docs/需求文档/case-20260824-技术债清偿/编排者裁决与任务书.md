# 编排者裁决 + 任务书（fast 档，代 PO/SE 合并）— case-20260824-技术债清偿

> 裁决人：team-orchestrator | 2026-08-24 | 来源：L3 收口（S2~S6/D3~D5）+ MCPMock（S1/S4/S5）+ 历次评审留痕
> 性质：纯技术债清偿，无新功能，零 DDL（全部应用层/前端）

## 任务清单（T1~T10，单 Dev fast 档）

| T | 来源 | 内容 | 验收 |
|---|------|------|------|
| T1 | L3-S2 | Risk/BusinessCase 的 edit 审计补 old→new 快照（对齐 ComplianceCheck 先例：P/I/category/owner 与金额/RICE 因子） | 审计 detail 含前后值，单测断言 |
| T2 | L3-S3 | risk/bizcase 的 transit UPDATE 追加 `.eq(status, from)` CAS；更新行数=0 重读抛 400"状态已变更" | 并发互覆用例（两次 transit 同 from，第二次 400） |
| T3 | L3-S4 | RiskController.toJsonRelated 改 ObjectMapper.writeValueAsString | 单测：控制字符入参正确转义 |
| T4 | L3-S5 | owner(64)/resolutionNote(500)/clauseRef(128)/frameworkName(128)/evidenceNote(1000)/rejectedReason(500) 长度前置校验 400 | 超长用例 400 指名字段 |
| T5 | 平台-S6 | GlobalExceptionHandler 补 HttpMessageNotReadableException → 400（"请求体格式错误"）；MethodArgumentNotReadable 同径 | 坏 JSON → 400 非 50000；既有测试回归 |
| T6 | L3-D3 | Standard transit 同 T2 模式 CAS | 同 T2 |
| T7 | L3-D4 | governance 各 ServiceImpl 的 edit/transit wrapper 增 `.set(update_by, 当前用户)`（对齐 V4~V8 审计列语义） | 落库断言 update_by 非空 |
| T8 | L3-D5 | risk-board.html 关联对象选择器 size=500 → 服务端分页拉取（或 size 上限 200+提示"输入关键词过滤"），防静默截断 | 前端逻辑+注释 |
| T9 | MCP-S1 | MCPController invoke 参数值长度上限（单值 10KB → -32602 结构化错误） | 超限用例 |
| T10 | MCP-S4+S5 | Mock CODE_INTERNAL 固定文案（明细留日志）+ 每桶上限 10k 条结构化错误 | 单测 |
| T11 | 商用化-Security S1 | bill_transit 资金审计改同步写（审计失败=流转失败回滚，杜绝"资金已变无审计"） | 审计失败注入→流转回滚用例 |
| T12 | 商用化-Security S2 | TenantContextFilter 删 X-Tenant-Id 头解析（仅 JWT 派生）——Security 评审"新增白名单即越权面"根除 | 头伪造用例无效化 + 全量回归（现有测试若依赖头需同步改 JWT 模拟） |
| T13 | 修复 Dev 发现 | LoginUser.set(null) 不清 TenantContext 的双 ThreadLocal 雷统一排查：全测试树 grep LoginUser.set 收尾改 clear()（TenantControllerTest 等同款写法） | grep 清单+改动点全绿 |

## 约束
- 全部零 DDL；不动 V1~V8；不改对外契约（错误码语义增强属兼容）
- 单 Dev fast 档：PO/SE 由本任务书代（来源留痕齐全），直接 Dev → Reviewer → QA 代验 → Ops
- 测试基线 743（Wave5 合入后），全绿为准
