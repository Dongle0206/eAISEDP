# 编排者裁决记录 — case-20260823-商用化

> 裁决人：team-orchestrator | 2026-08-23 | 依据：PRD v1.0 开放问题 Q1~Q6

| Q | 议题 | 裁决 |
|---|------|------|
| Q1 | 表名与租户-套餐绑定 | `t_plan`（套餐）/ `t_invoice`（账单）/ `t_incident`（事故）。绑定：**t_tenant 加 `plan_code` 列**（V8 动态判断加列，引用 t_plan.code）；变更历史**不建独立表**——审计留痕（tenant_edition_change 先例）+ 账单内套餐快照（B1）已构成完整追溯链 |
| Q2 | 出账时机 | **定时任务**（每月 1 日 02:00 @Scheduled，幂等：账期+租户唯一键防重）+ **platform_admin 手动补生成端点**（指定账期）。测试 profile 关闭 @Scheduled（CapabilityLoader 先例） |
| Q3 | 超量粒度 | 千 token ceil（B5 冻结口径）；case/storage 维度本期不扩展（范围外，PRD 已列） |
| Q4 | 变更生效 | **立即生效 + 月末快照定价**（B1 冻结，不改写）。降级水位：降级立即生效，当月配额不回退（按已用继续），超量按**新套餐单价**计——简单可解释，写入账单明细 |
| Q5 | executive 费用中心 | **不可见**（费用是管理层信息；executive 治理只读先例 US-7，权限矩阵维持 PO 版） |
| Q6 | 月中新建 | **首月按剩余天数折算**（月价 ÷ 当月天数 × 剩余天数含当天，HALF_UP 2 位）；trial 转正赠送当月口径不变（B2/B3） |

## 附带约束（下发 SE/DBA）
1. V8：3 新表 CREATE IF NOT EXISTS + t_tenant 加 plan_code 列（**information_schema 动态判断**，V4 r4 规范）+ seed 9 权限原子（1081~1089）+ 18 授权行（2203~2220，矩阵按 PRD）；V9 从 1090/2221 续排
2. 账单幂等：uk(tenant_id, period) 防重出账；金额全 DECIMAL(14,2) + Service 校验上限（L3 收口 S1 教训镜像）
3. U2 向后兼容（AC-F1.8 回归不可省）；出账定时任务线程池独立（不抢 orchestrationExecutor）
4. 计算逻辑（折算/超量/舍入）纯静态 BillingCalculator 先行锁口径（L3 双计算器先例）
5. 三表租户级（t_plan 例外：平台级 tenant_id=0 进 IGNORE_TABLES——套餐是平台资产；t_invoice/t_incident 租户级）
