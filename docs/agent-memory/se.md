# SE 角色经验库

### [2026-08-18] 引擎层零数据源依赖：注入在编排入口预解析
`tags: 架构, 依赖单向, severity: high`
- 下行注入在 OrchestrationService 入口解析一次预渲染成 String，ContextAssembler 只拼接不查库——P3 依赖单向 + 单角色派生不被牵连 + 断点续跑天然复用快照

### [2026-08-18] 上行汇总用事件+全量重算而非同步调用
`tags: 事件, 幂等, severity: high`
- 状态机 transit 内同步调用会让汇总抛错打断主流程；Spring 事件 + @Async 独立线程池 + 全量重算（天然幂等/并发安全）

### [2026-08-18] 门禁规则加载刻意不做缓存
`tags: 门禁, 快照, severity: mid`
- 启动时一次读库生成不可变快照：新编排立即生效、在跑编排零影响，消除缓存一致性一类问题
